{
  "name": "scheduler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cross-env BROWSER=none  npm run reactrun\" \"npm run electronrun\"",
    "reactrun": "\" cd src && npm run start\" ",
    "electronrun": "wait-on tcp:127.0.0.1:3000 && electron .",
    "build": "cd src && npm run build && cd .. && electron-builder"
  },
  "keywords": [],
  "author": "Radosław Boczoń",
  "license": "ISC",
  "devDependencies": {
    "electron": "^25.3.0"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0"
  }
}
